@model IEnumerable<CalendarioGerarOrg.Models.recesso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idcidadeNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idcidadeNavigation.idcidade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idrecesso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idrecesso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idrecesso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
